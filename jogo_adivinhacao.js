let inicio = 1;
let fim = 50;
let numeros_ecreto;
let numero_sorteado = Math.random * (fim - inicio + 1) + inicio;
  
while (numero_secreto <= numero_sorteado){
    console.log(numero_sorteado);
    numero_sorteado ++
    
}
